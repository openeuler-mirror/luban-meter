# LuBan-Meter术语

LuBan-Meter在已准备好的运行环境中执行评测，保存原始事实并计算指标。

## 评测与运行

**评测类别（Category）**：按评测目标划分的一组场景，包括生成性能和模型服务质量。
_Avoid_：用module同时指评测类别和Python模块。

**模型服务质量（Model Service Quality）**：在线模型服务在指定数据集、提示词和
评分协议下返回结果的正确性与任务完成质量。评测对象是实际调用的模型服务。

**评测场景（Scenario）**：具有明确被测对象、输入条件和观察边界的评测单元，
例如在线服务性能、离线引擎性能或C-Eval准确率。
_Avoid_：用benchmark同时指场景名称、脚本路径、定义对象和运行结果。

**场景定义（Scenario Definition）**：一个场景的身份及其采集、计算职责。
_Avoid_：将场景名称字符串与场景定义对象混称为benchmark。

**运行（Run）**：采用一组确定配置执行一个评测场景的过程。

**测试条件组合（Case）**：一次运行中固定输入长度、输出长度、负载等条件的
独立统计单元。

**评测套件（Suite）**：有序执行多个评测任务的集合，各任务保留独立结果。

## 模型与结果

**服务模型名称（Served Model Name）**：在线推理服务用于选择被测模型的标识。
_Avoid_：把模型名称字符串、模型实例和模型信息都称为model。

**模型引用（Model Reference）**：用于加载模型的本地权重路径或模型名称。

**模型信息（Model Info）**：结果中记录的模型名称、路径及其他模型身份信息。

**原始结果（Raw Result）**：采集阶段保存的请求、样本、时间戳、环境和错误事实。

**运行结果（Run Result）**：由原始事实计算得到的指标，以及该次运行的状态、
参数、环境和产物信息。
